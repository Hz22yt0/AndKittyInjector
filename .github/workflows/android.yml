name: Android CI

on:
  workflow_dispatch:


jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Download Command Line Tools
      run: |
          

          cd $ANDROID_HOME/AndKittyInjector/
          cd $ANDROID_HOME/AndKittyInjector1/
